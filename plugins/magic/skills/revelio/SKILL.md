---
name: revelio
description: |
  Emit a context refresh, then STOP. Use when user called `revelio` or `revelio!` directly
---

Emit a context refresh, then STOP. Surface what's gone stale or buried mid-conversation that's load-bearing for the next step: recent activity, current work state, operator constraints from earlier turns, prioritized next-actions (3–5 items). Skip what's already in recent turns — re-emitting it is noise, not refresh. Do NOT begin work; operator continues explicitly, or ask a corrective instruction to proceed. Does NOT carry across invocations.
